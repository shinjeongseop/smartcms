server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index home.html;

    location = / {
        try_files /home.html =404;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
